;; og:preserve-this bring-up: the two remaining .o entries converted to .go raw copies
;; (brdroom recipe, see jungles.gd). havenb is the haven city end of the
;; havjung-wide-start want set: 'havenb is not small-center/tiny-center flavored (plain
;; 'tiny, :borrow #f in level-info.gc), so it loads as a direct want like junglea or
;; jungleg do, with no borrow-info pairing needed (#151).
("HVB.DGO"
 ("tpage-658.go"
  "tpage-968.go"
  "havenb-vis.go"
 ))
