;; og:preserve-this bring-up: tpage-2905 and the ices bsp converted to .go raw copies
;; (brdroom recipe, see jungles.gd). This track has NO sprite tpage: ICES ships four
;; pages (tfrag, pris, water, minimap), so the #101 stranded-sprite defect has no
;; icelands analogue. peak-snowball-ag's type has no deftype anywhere (commented out at
;; decompiler/config/jakx/all-types.gc:61768); its five ices entities birth through the
;; guarded entity-actor path as non-fatal no-proper-process-type lines. Same dormant
;; widen-trap family as icx.gd's ice-flag note: ICETOURS and PEAKS load ices-family data
;; without peak-obs in their want sets, so whoever lands peak-snowball must arm those
;; DGOs' code objects in the same change.
("ICES.DGO"
 ("tpage-2905.go"
  "tpage-3125.go"
  "tpage-2963.go"
  "tpage-3102.go"
  "peak-snowball-ag.go"
  "ices.go"
 ))
