;;-*-Lisp-*-
(in-package goal)

;; name: default-menu.gc
;; name in dgo: default-menu
;; dgos: ENGINE, GAME

;; DECOMP BEGINS

;; og:preserve-this bring-up stub (#115): retail's body only drives debug-menu
;; contexts and returns #f (jak3's defun at its default-menu.gc:6845 is the model;
;; none of that machinery exists in jakx: zero debug-menu-context-send-msg hits).
;; Landing it for real is the whole debug-menu system (retail default-menu.go is
;; 159 KB). Until then the symbol linked 0 and set-master-mode's *debug-segment*
;; call crashed goal-0 on every pause or fullscreen toggle (the #115 attribution).
(defun menu-respond-to-pause ()
  #f
  )

