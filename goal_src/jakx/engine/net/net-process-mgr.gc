;;-*-Lisp-*-
(in-package goal)

;; name: net-process-mgr.gc
;; name in dgo: net-process-mgr
;; dgos: ENGINE, GAME

;; DECOMP BEGINS

;; og:preserve-this bring-up stub: real body adjusts locally-owned net entities after
;; level activation, gated on net-world state that is inactive in single-player. Callers
;; discard the result.
(defun tweak-local-net-entities ()
  0
  )

;; og:preserve-this bring-up: the manager process never spawns before the net leg, but
;; process-nettable m22 reads (not *net-process-mgr*) as its single-player short-circuit
;; and the m34 gate tests it, so the symbol must exist and read #f rather than 0
(define *net-process-mgr* (the-as net-process-mgr #f))
;; (both of the above retire when the net-process-mgr leg lands the object's real body)

;; og:preserve-this bring-up (#116): set-proto-vis! ORs *net-task-mask* into every
;; active level's task-mask; retail's writer lives in the unlanded net code, so the
;; single-player default of no net task bits is defined here at the net home. Retires
;; when the net leg lands the real writer.
(define *net-task-mask* (task-mask))
