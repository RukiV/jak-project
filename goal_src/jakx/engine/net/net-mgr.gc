;;-*-Lisp-*-
(in-package goal)

;; name: net-mgr.gc
;; name in dgo: net-mgr
;; dgos: ENGINE, GAME

;; DECOMP BEGINS

;; og:preserve-this bring-up stub: (code init-state lobby-menu-manager)
;; (menu-road rung 2) calls this with self and the literal int 15,
;; discarding the return value; a bare define-extern with no defun
;; anywhere would link the call to 0 (check_extern_defun's own FAIL
;; class, the issue-531 defect this mirror's own doctrine names
;; explicitly).
(defun net-mgr-notify-connect ((arg0 process) (arg1 int))
  (none)
  )

