;;-*-Lisp-*-
(in-package goal)

;; name: net-mgr-medius-clans.gc
;; name in dgo: net-mgr-medius-clans
;; dgos: ENGINE, GAME

;; DECOMP BEGINS

;; og:preserve-this bring-up stub: (code init-state lobby-menu-manager)
;; (menu-road rung 2, goal_src/jakx/engine/ui/menu/lobby-menu-manager.gc)
;; calls this with zero explicit args and discards the return value; a bare
;; define-extern with no defun anywhere would link the call to 0
;; (check_extern_defun's own FAIL class, the issue-531 defect this
;; mirror's own doctrine names explicitly), so this stub answers "nothing
;; to apply yet" until the real object (this file's own content, still
;; otherwise a 9-line stub) lands in a later rung.
(defun apply-clan-deltas ()
  (none)
  )

