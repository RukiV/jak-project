;;-*-Lisp-*-
(in-package goal)

;; name: rigid-body-object.gc
;; name in dgo: rigid-body-object
;; dgos: ENGINE, GAME

;; DECOMP BEGINS

;; og:preserve-this rigid-body rung: the rigid-body-object rung's own hazard
;; note above (every rbody dispatch jumping to zero because rigid-body.gc was
;; an empty stub) is resolved: rigid-body.gc now lands rigid-body-control's
;; methods 9-40 (see that file's own header and rigid-body-h.gc's :methods
;; comments for the full per-slot evidence; a handful stay held under
;; hacks.jsonc's no_type_analysis_functions_by_name, named there). Landing
;; those bodies changed several of THIS file's own already-compiled call
;; sites: methods 10, 20, and 30 (numbered by their final, corrected slots,
;; not the placeholder guesses a prior rung decoded against) all moved from
;; a zero-arg method-of-object dispatch with dead-looking expressions beside
;; it to a direct call with those same expressions restored as real
;; arguments, the same bootstrapping-decode-artifact class documented at
;; each fixed method below (62, 63, 64, 72, 87). rigid-body-control-method-9
;; and (method 9 rigid-body-info) stay held (hacks.jsonc), so calls that
;; reach them (through method 10, landed) still link as zero at runtime.
;; Second target, same rung: method 82 (attack-handler) is UNHELD and
;; landed below, its own 4-arg shape confirmed by a clean decode, and
;; method 81's own 'attack-case dispatch to it is restored the same way;
;; construction-obs-h.gc's override and tail-call are widened together
;; with this change. Flagged for the boot smoke, since this touches a
;; GAME.CGO object.

;; definition for method 57 of type rigid-body-object
(defmethod process-focusable-method-57 ((this rigid-body-object))
  (-> this info info inv-mass)
  )

;; definition for method 68 of type rigid-body-object
;; WARN: Return type mismatch int vs none.
(defmethod rigid-body-object-method-68 ((this rigid-body-object) (arg0 object) (arg1 object))
  (set! (-> this info) (the-as rigid-body-object-constants arg0))
  (set! (-> this rbody info) (-> (the-as rigid-body-object-constants arg0) info))
  (if arg1
      (rigid-body-info-method-9 (-> this info info))
      )
  0
  (none)
  )

;; definition for method 69 of type rigid-body-object
;; WARN: Return type mismatch int vs none.
(defmethod rigid-body-object-method-69 ((this rigid-body-object))
  (let ((a1-0 (rigid-body-object-constants-method-9 (-> this info))))
    (if (and a1-0 (!= a1-0 (-> this info)))
        (rigid-body-object-method-68 this a1-0 #t)
        )
    )
  (set! (-> this rbody force-callback)
        (the-as (function rigid-body-object float none) (method-of-object this rigid-body-object-method-62))
        )
  0
  (none)
  )

;; definition for method 62 of type rigid-body-object
;; WARN: Return type mismatch int vs none.
;; og:preserve-this rigid-body rung: RESTORED the real add-force! call,
;; reversing the rigid-body-object rung's own hazard note. rigid-body-control-
;; method-20 was an empty stub at the time of that rung (this file's own
;; dependency, then unlanded), so its own placeholder "()" arity forced this
;; call to decode with zero args and left the freshly-built gravity vector
;; computed and discarded. Now that rigid-body.gc lands add-force!'s real
;; (_type_ vector) none signature, a fresh decode of this method shows the
;; vector actually passed through. arg0 stays unused in this base body, the
;; same class as method 68/79's own unused trailing args; a wvehicle-specific
;; override of this same force-callback slot (wvehicle-physics.gc) reads its
;; own explicit arg, proving the slot genuinely carries one.
(defmethod rigid-body-object-method-62 ((this rigid-body-object) (arg0 float))
  (let ((a1-1 (new 'stack-no-clear 'vector)))
    (vector-reset! a1-1)
    (set! (-> a1-1 y) (* -1.0 (-> this info info gravity) (-> this rbody info mass)))
    (rigid-body-control-method-20 (-> this rbody) a1-1)
    )
  0
  (none)
  )

;; definition for method 63 of type rigid-body-object
;; WARN: Return type mismatch int vs none.
;; og:preserve-this rigid-body rung: RESTORED the real call shape. Same root
;; cause as method 62 above: rigid-body-control-method-10 was declared with a
;; wrong, too-narrow two-float signature (a prior rung's call-site evidence
;; was drawn from this exact stale decode, a bootstrapping loop), which
;; decoded this method's own "this" argument as a bogus float cast and left
;; max-time-step as a discarded dead expression. rigid-body.gc's rung
;; restores method-10's real (rigid-body-object float float) shape from the
;; callee's own body evidence; this call now decodes clean with "this" and
;; max-time-step as real arguments, matching jak3's own
;; rigid-body-object-method-32 call shape.
(defmethod rigid-body-object-method-63 ((this rigid-body-object))
  (rigid-body-control-method-10 (-> this rbody) this (seconds-per-frame) (-> this max-time-step))
  0
  (none)
  )

;; definition for method 85 of type rigid-body-object
;; WARN: Return type mismatch int vs none.
(defmethod rigid-body-object-method-85 ((this rigid-body-object))
  0
  (none)
  )

;; definition for method 86 of type rigid-body-object
;; WARN: Return type mismatch int vs none.
(defmethod rigid-body-object-method-86 ((this rigid-body-object))
  0
  (none)
  )

;; definition for method 87 of type rigid-body-object
;; WARN: Return type mismatch int vs none.
;; og:preserve-this rigid-body rung: RESTORED the real call shape, the same
;; root cause and fix as method 63 above (rigid-body-control-method-10's
;; corrected signature), matching jak3's own rigid-body-object-method-54
;; call shape.
(defmethod rigid-body-object-method-87 ((this rigid-body-object))
  (rigid-body-control-method-10 (-> this rbody) this (-> this rbody time-remaining) (-> this max-time-step))
  0
  (none)
  )

;; definition for method 88 of type rigid-body-object
;; WARN: Return type mismatch int vs none.
(defmethod rigid-body-object-method-88 ((this rigid-body-object))
  0
  (none)
  )

;; definition for method 67 of type rigid-body-object
;; WARN: Return type mismatch int vs none.
(defmethod rigid-body-object-method-67 ((this rigid-body-object))
  (go (method-of-object this idle))
  0
  (none)
  )

;; definition for method 64 of type rigid-body-object
;; WARN: Return type mismatch int vs none.
;; og:preserve-this rigid-body rung: RESTORED the real init! call and the
;; missing third "new" argument, reversing the rigid-body-object rung's own
;; hazard note (both callees were unlanded stubs at that rung, forcing this
;; decode against placeholder zero/two-arg signatures). Two separate
;; bootstrapping artifacts land together here: "new"'s own process argument
;; ((-> v0-0 process) arg0, rigid-body.gc's method 0) was silently dropped,
;; and the call this rung's own decode had labeled "rigid-body-control-
;; method-29" (a jak3-position guess, then debug-print-info-shaped) is
;; rigid-body-control's real init! (5 args including self), with what this
;; rung's own decode had rendered as three dead expressions
;; ((-> this root trans), (-> this root rot), (method-of-object this
;; rigid-body-object-method-62)) restored as init!'s own vector/quaternion/
;; callback arguments, matching jak3's own alloc-rbody-control! call shape.
(defmethod rigid-body-object-method-64 ((this rigid-body-object) (arg0 object))
  (set! (-> this info) (the-as rigid-body-object-constants arg0))
  (set! (-> this rbody) (new 'process 'rigid-body-control this))
  (update-transforms (-> this root))
  (rigid-body-control-method-29
    (-> this rbody)
    (-> (the-as rigid-body-object-constants arg0) info)
    (-> this root trans)
    (the-as quaternion (-> this root rot))
    (the-as (function rigid-body-object float) (method-of-object this rigid-body-object-method-62))
    )
  (rigid-body-object-method-68 this (the-as rigid-body-object-constants arg0) #t)
  (rigid-body-object-method-69 this)
  (set! (-> this max-time-step) (-> (the-as rigid-body-object-constants arg0) info max-time-step))
  (set! (-> this root max-iteration-count) (the-as uint 4))
  (when (nonzero? (-> this skel))
    (let ((v1-17 (-> this skel root-channel 0)))
      (set! (-> v1-17 num-func) num-func-identity)
      (set! (-> v1-17 frame-num) 0.0)
      )
    (ja-post)
    )
  0
  (none)
  )

;; definition for method 65 of type rigid-body-object
;; WARN: Return type mismatch int vs none.
(defmethod rigid-body-object-method-65 ((this rigid-body-object))
  (let ((s5-0 (new 'process 'collide-shape-moving this (collide-list-enum hit-by-player))))
    (set! (-> s5-0 dynam) (copy *standard-dynamics* 'process))
    (set! (-> s5-0 reaction) cshape-reaction-default)
    (set! (-> s5-0 no-reaction)
          (the-as (function collide-shape-moving collide-query vector vector object) nothing)
          )
    (let ((s4-0 (new 'process 'collide-shape-prim-sphere s5-0 (the-as uint 0))))
      (set! (-> s4-0 prim-core collide-as) (collide-spec pusher))
      (set! (-> s4-0 prim-core collide-with) (collide-spec jak player-list))
      (set! (-> s4-0 prim-core action) (collide-action solid))
      (set! (-> s4-0 transform-index) 3)
      (set-vector! (-> s4-0 local-sphere) 0.0 0.0 0.0 12288.0)
      (set! (-> s5-0 total-prims) (the-as uint 1))
      (set! (-> s5-0 root-prim) s4-0)
      )
    (pusher-init s5-0)
    (set! (-> s5-0 nav-radius) (* 0.75 (-> s5-0 root-prim local-sphere w)))
    (let ((v1-16 (-> s5-0 root-prim)))
      (set! (-> s5-0 backup-collide-as) (-> v1-16 prim-core collide-as))
      (set! (-> s5-0 backup-collide-with) (-> v1-16 prim-core collide-with))
      )
    (set! (-> this root) s5-0)
    )
  0
  (none)
  )

;; definition for symbol *rigid-body-object-constants*, type rigid-body-object-constants
(define *rigid-body-object-constants*
  (new 'static 'rigid-body-object-constants
    :info (new 'static 'rigid-body-info
      :max-time-step 0.033333335
      :mass 2.0
      :inv-mass 0.5
      :bounce-factor 0.5
      :friction-factor 0.1
      :cm-offset-joint (new 'static 'vector :data (new 'static 'array float 4 0.0 0.0 0.0 1.0))
      :inertial-tensor-box (new 'static 'array meters 3 (meters 4) (meters 4) (meters 4))
      :gravity 163840.0
      )
    :name '*rigid-body-object-constants*
    )
  )

;; definition for method 66 of type rigid-body-object
;; WARN: Return type mismatch int vs none.
(defmethod rigid-body-object-method-66 ((this rigid-body-object))
  (rigid-body-object-method-64 this *rigid-body-object-constants*)
  0
  (none)
  )

;; definition for method 11 of type rigid-body-object
;; WARN: Return type mismatch int vs object.
(defmethod init-from-entity! ((this rigid-body-object) (arg0 entity-actor))
  (rigid-body-object-method-65 this)
  (process-drawable-from-entity! this arg0)
  (rigid-body-object-method-66 this)
  (rigid-body-object-method-67 this)
  0
  )

;; definition for method 70 of type rigid-body-object
;; WARN: Return type mismatch int vs none.
(defmethod rigid-body-object-method-70 ((this rigid-body-object))
  0
  (none)
  )

;; definition for method 61 of type rigid-body-object
;; WARN: Return type mismatch int vs none.
(defmethod rigid-body-object-method-61 ((this rigid-body-object))
  (ja-post)
  0
  (none)
  )

;; definition for method 71 of type rigid-body-object
;; WARN: Return type mismatch int vs none.
(defmethod rigid-body-object-method-71 ((this rigid-body-object))
  (rigid-body-object-method-63 this)
  (rigid-body-object-method-70 this)
  (rigid-body-control-method-11 (-> this rbody) (the-as collide-shape-moving (-> this root)))
  (rigid-body-object-method-61 this)
  (update-transforms (-> this root))
  0
  (none)
  )

;; definition for method 74 of type rigid-body-object
;; WARN: Return type mismatch int vs none.
(defmethod rigid-body-object-method-74 ((this rigid-body-object))
  (logior! (-> this flags) 1)
  (let ((v1-3 (-> this root root-prim)))
    (set! (-> v1-3 prim-core collide-as) (-> this root backup-collide-as))
    (set! (-> v1-3 prim-core collide-with) (-> this root backup-collide-with))
    )
  0
  (none)
  )

;; definition for method 75 of type rigid-body-object
;; WARN: Return type mismatch int vs none.
(defmethod rigid-body-object-method-75 ((this rigid-body-object))
  (logand! (-> this flags) -2)
  (let ((v1-3 (-> this root root-prim)))
    (set! (-> v1-3 prim-core collide-as) (collide-spec))
    (set! (-> v1-3 prim-core collide-with) (collide-spec))
    )
  0
  0
  (none)
  )

;; definition for method 72 of type rigid-body-object
;; WARN: Return type mismatch int vs none.
;; og:preserve-this rigid-body rung: RESTORED the real call shape, the same
;; bootstrapping-decode-artifact class as methods 62/63/64/87 above:
;; rigid-body-control-method-30 was declared with a wrong, too-narrow
;; (self+object, 2-arg) signature at the time of the rigid-body-object rung,
;; which decoded this call as a bare zero-arg dispatch and left the position/
;; rotation reads as dead expressions. rigid-body.gc's rung restores
;; method-30's real (vector quaternion) shape from the callee's own body
;; evidence; those two dead expressions are init!'s own vector/quaternion
;; arguments, matching jak3's own apply-momentum! call shape.
(defmethod rigid-body-object-method-72 ((this rigid-body-object))
  (when (not (logtest? (-> this rbody flags) 2))
    (logior! (-> this rbody flags) 2)
    (rigid-body-control-method-30 (-> this rbody) (-> this root trans) (the-as quaternion (-> this root rot)))
    (vector-float*! (-> this rbody lin-momentum) (-> this root transv) (-> this info info mass))
    (vector-reset! (-> this rbody ang-momentum))
    )
  0
  (none)
  )

;; definition for method 73 of type rigid-body-object
;; WARN: Return type mismatch int vs none.
(defmethod rigid-body-object-method-73 ((this rigid-body-object))
  (logand! (-> this rbody flags) -3)
  0
  (none)
  )

;; definition for method 76 of type rigid-body-object
;; WARN: Return type mismatch int vs none.
(defmethod rigid-body-object-method-76 ((this rigid-body-object))
  (logior! (-> this flags) 2)
  (set! (-> this disturbed-time) (the-as uint (current-time)))
  (if (not (logtest? (-> this rbody flags) 2))
      (rigid-body-object-method-72 this)
      )
  0
  (none)
  )

;; definition for method 77 of type rigid-body-object
;; WARN: Return type mismatch int vs none.
(defmethod rigid-body-object-method-77 ((this rigid-body-object))
  (go (method-of-object this active))
  0
  (none)
  )

;; definition for method 78 of type rigid-body-object
;; WARN: Return type mismatch int vs none.
(defmethod rigid-body-object-method-78 ((this rigid-body-object))
  0
  (none)
  )

;; definition for method 80 of type rigid-body-object
;; WARN: Return type mismatch int vs none.
(defmethod rigid-body-object-method-80 ((this rigid-body-object) (arg0 rigid-body-impact))
  0
  (none)
  )

;; definition for method 84 of type rigid-body-object
;; INFO: Used lq/sq
;; WARN: Return type mismatch int vs none.
(defmethod rigid-body-object-method-84 ((this rigid-body-object) (arg0 rigid-body-impact) (arg1 touching-shapes-entry))
  (set! (-> arg0 process) #f)
  (set! (-> arg0 prim-id) (the-as uint 0))
  (vector-reset! (-> arg0 normal))
  (vector-reset! (-> arg0 velocity))
  (set! (-> arg0 point quad) (-> this root trans quad))
  (when arg1
    (let ((s3-0 (-> arg1 head)))
      (when s3-0
        (get-intersect-point (-> arg0 point) s3-0 (-> this root) arg1)
        (let ((s5-1 (get-touched-prim s3-0 (-> this root) arg1)))
          (when s5-1
            (set! (-> arg0 prim-id) (-> s5-1 prim-id))
            (vector-! (-> arg0 normal) (-> arg0 point) (-> s5-1 prim-core world-sphere))
            (vector-normalize! (-> arg0 normal) 1.0)
            (vector+float*!
              (-> arg0 point)
              (-> s5-1 prim-core world-sphere)
              (-> arg0 normal)
              (-> s5-1 prim-core world-sphere w)
              )
            )
          )
        )
      )
    )
  0
  (none)
  )

;; definition for method 79 of type rigid-body-object
;; WARN: Return type mismatch int vs none.
(defmethod rigid-body-object-method-79 ((this rigid-body-object) (arg0 rigid-body-impact) (arg1 attack-info))
  (local-vars (f1-1 number))
  0.0
  (let ((f0-1 0.0))
    (cond
      ((logtest? (attack-mask vehicle-damage-factor) (-> arg1 mask))
       (set! f1-1 (* (-> arg1 damage) (-> arg1 vehicle-damage-factor)))
       (if (logtest? (attack-mask vehicle-impulse-factor) (-> arg1 mask))
           (set! f0-1 (* 49152.0 (-> arg1 vehicle-impulse-factor) (-> arg1 damage)))
           )
       0
       )
      (else
        (set! f0-1 8192.0)
        (set! f1-1 2.0)
        )
      )
    (set! (-> arg0 impulse) f0-1)
    )
  (rigid-body-object-method-78 this)
  0
  (none)
  )

;; definition for method 82 of type rigid-body-object
;; INFO: Used lq/sq
;; WARN: Return type mismatch symbol vs none.
;; og:preserve-this rigid-body rung (second target): RESTORED to the full
;; 4-arg attack-handler shape, reversing the rigid-body-object rung's own
;; hold. That rung's own raw evidence (two extra registers read past the
;; held two-arg signature) already matched jak3's attack-handler (slot 50,
;; +32 shift) at four args; this rung lands the body from a clean decode
;; under the widened signature and updates construction-obs-h.gc's own
;; override and tail-call together in the same change, so the arity-checked
;; method-of-type call there stays consistent. Reached through
;; rigid-body-object-method-81's own 'attack case.
(defmethod rigid-body-object-method-82 ((this rigid-body-object)
                                       (arg0 process-drawable)
                                       (arg1 attack-info)
                                       (arg2 touching-shapes-entry)
                                       (arg3 penetrate)
                                       )
  (when arg2
    (let ((s5-0 (new 'stack-no-clear 'rigid-body-impact)))
      (rigid-body-object-method-84 this s5-0 arg2)
      (if (logtest? (attack-mask attacker-velocity) (-> arg1 mask))
          (set! (-> s5-0 velocity quad) (-> arg1 attacker-velocity quad))
          (vector-! (-> s5-0 velocity) (-> s5-0 point) (-> arg0 root trans))
          )
      (rigid-body-object-method-79 this s5-0 arg1)
      (rigid-body-object-method-76 this)
      (let ((s4-1 (new 'stack-no-clear 'vector)))
        (set! (-> s4-1 quad) (-> s5-0 velocity quad))
        (vector-normalize! s4-1 1.0)
        (vector-float*! s4-1 s4-1 (-> s5-0 impulse))
        (rigid-body-control-method-18 (-> this rbody) (-> s5-0 point) s4-1)
        (rigid-body-control-method-12 (-> this rbody) 1.0)
        (rigid-body-control-method-13 (-> this rbody))
        (when #f
          (add-debug-x #t (bucket-id bucket785) (-> s5-0 point) *color-blue*)
          (add-debug-vector #t (bucket-id bucket785) (-> s5-0 point) s4-1 (meters 0.00024414062) *color-blue*)
          )
        )
      (rigid-body-object-method-80 this s5-0)
      )
    (if (and (-> this next-state) (= (-> this next-state name) 'idle))
        (rigid-body-object-method-77 this)
        )
    #t
    )
  (none)
  )

;; definition for method 83 of type rigid-body-object
;; INFO: Used lq/sq
(defmethod rigid-body-object-method-83 ((this rigid-body-object) (arg0 process-focusable) (arg1 touching-shapes-entry))
  (let ((s5-0 (new 'stack-no-clear 'inline-array 'vector 9)))
    (set! (-> s5-0 7 x) (process-focusable-method-57 arg0))
    (rigid-body-object-method-84 this (the-as rigid-body-impact (-> s5-0 0)) arg1)
    (if (logtest? (-> this rbody flags) 2)
        (rigid-body-control-method-24 (-> this rbody) (-> s5-0 0) (-> s5-0 2))
        (set! (-> s5-0 2 quad) (-> this root transv quad))
        )
    (let ((v1-10 (-> arg0 root)))
      (set! (-> s5-0 5 quad) (-> v1-10 transv quad))
      (vector-! (-> s5-0 2) (-> v1-10 transv) (-> s5-0 2))
      )
    (let ((f0-2 (vector-dot (-> s5-0 2) (-> s5-0 1))))
      (when (< f0-2 0.0)
        (set! (-> s5-0 3 x) (/ f0-2 (+ (-> s5-0 7 x) (-> this info info inv-mass))))
        (vector+float*! (-> s5-0 5) (-> s5-0 5) (-> s5-0 1) (* -3.1 (-> s5-0 7 x) (-> s5-0 3 x)))
        (set! (-> s5-0 5 y) (fmax (* 49152.0 (-> s5-0 7 x)) (-> s5-0 5 y)))
        (rigid-body-object-method-76 this)
        (vector-float*! (-> s5-0 6) (-> s5-0 1) (-> s5-0 3 x))
        (rigid-body-control-method-18 (-> this rbody) (-> s5-0 0) (-> s5-0 6))
        (rigid-body-control-method-12 (-> this rbody) 1.0)
        (rigid-body-control-method-13 (-> this rbody))
        (when #f
          (add-debug-x #t (bucket-id bucket785) (-> s5-0 0) *color-blue*)
          (add-debug-vector #t (bucket-id bucket785) (-> s5-0 0) (-> s5-0 1) (- (-> s5-0 3 x)) *color-blue*)
          )
        (rigid-body-object-method-80 this (the-as rigid-body-impact (-> s5-0 0)))
        (if (and (-> this next-state) (= (-> this next-state name) 'idle))
            (rigid-body-object-method-77 this)
            )
        )
      )
    )
  #t
  )

;; definition for method 81 of type rigid-body-object
;; og:preserve-this rigid-body rung (second target): the 'attack case's own
;; dispatch to method 82 is RESTORED to its real 5-arg call shape (t0-1, the
;; penetrate result, moved into the let-binding rather than a discarded bare
;; call, and the vtable indirection replaced with a direct call now that
;; method 82 has a real signature), the same bootstrapping-decode-artifact
;; class as rigid-body-control's own fixed call sites in the first target.
(defmethod rigid-body-object-method-81 ((this rigid-body-object) (arg0 process) (arg1 int) (arg2 symbol) (arg3 event-message-block))
  (case arg2
    (('impact-impulse)
     (let ((s5-1 (-> arg3 param 0)))
       (if (!= this arg0)
           (rigid-body-object-method-76 this)
           )
       (rigid-body-object-method-80 this (the-as rigid-body-impact s5-1))
       )
     (if (and (-> this next-state) (= (-> this next-state name) 'idle))
         (rigid-body-object-method-77 this)
         )
     #t
     )
    (('touched)
     (if (= this *debug-actor*)
         (format *stdcon* "rigid-body-object got touched~%")
         )
     (when (zero? (-> (the-as process-focusable arg0) rbody))
       (let ((a1-4 (as-type arg0 process-focusable)))
         (if a1-4
             (rigid-body-object-method-83
               this
               (the-as process-focusable a1-4)
               (the-as touching-shapes-entry (-> arg3 param 0))
               )
             )
         )
       )
     )
    (('attack)
     (let ((s3-0 (the-as object (-> arg3 param 1)))
           (t0-1 (get-penetrate-using-from-attack-event (the-as process-drawable arg0) arg3))
           )
       (when (!= (-> (the-as attack-info s3-0) id) (-> this incoming-attack-id))
         (set! (-> this incoming-attack-id) (-> (the-as attack-info s3-0) id))
         (rigid-body-object-method-82
           this
           (the-as process-drawable arg0)
           (the-as attack-info s3-0)
           (the-as touching-shapes-entry (-> arg3 param 0))
           t0-1
           )
         )
       )
     )
    (('enable-physics)
     (rigid-body-object-method-76 this)
     #t
     )
    )
  )

;; definition for function rigid-body-object-event-handler
(defbehavior rigid-body-object-event-handler rigid-body-object ((arg0 process) (arg1 int) (arg2 symbol) (arg3 event-message-block))
  (rigid-body-object-method-81 self arg0 arg1 arg2 arg3)
  )

;; failed to figure out what this is:
(defstate idle (rigid-body-object)
  :virtual #t
  :event rigid-body-object-event-handler
  :code sleep-code
  )

;; failed to figure out what this is:
(defstate active (rigid-body-object)
  :virtual #t
  :event rigid-body-object-event-handler
  :enter (behavior ()
    (logior! (-> self rbody flags) 16)
    )
  :code sleep-code
  :post (behavior ()
    (rigid-body-object-method-71 self)
    )
  )
