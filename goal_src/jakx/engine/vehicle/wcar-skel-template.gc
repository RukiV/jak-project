;;-*-Lisp-*-
(in-package goal)

;; name: wcar-skel-template.gc
;; name in dgo: wcar-skel-template
;; dgos: ENGINE, GAME

;; DECOMP BEGINS

;; og:preserve-this this object's own body is not landed yet, but every wcar body
;; calls get-car-skel-part, which this object defines, so build-first needs the
;; declaration here. Signature copied from decompiler/config/jakx/all-types.gc.
;; Drop this line when the object itself lands.
(define-extern get-car-skel-part (function level string skeleton-group skeleton-group))
