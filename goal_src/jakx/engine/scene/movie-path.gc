;;-*-Lisp-*-
(in-package goal)

;; name: movie-path.gc
;; name in dgo: movie-path
;; dgos: ENGINE, GAME

;; eco is referenced only as a type argument to kill-by-type-inherited (the
;; 'record event's cleanup sweep) and is not landed anywhere in this tree yet.
(declare-type eco process-drawable)

;; DECOMP BEGINS

;; og:preserve-this movie-path decodes clean and its landing is held back on purpose:
;; movie-path entities exist in the boot levels and their init dispatches path-control
;; methods 9 and 18 (total-distance) whose implementations live in path.gc, still a stub,
;; so the slots are empty at runtime and the first spawn crashed the boot (bisected on
;; 2026-08-17: stack scan movie-path, gkernel, entity; rip 0). Land this file together
;; with path.gc, from the same decode; the reference test carries the full text.
