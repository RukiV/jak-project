;;-*-Lisp-*-
(in-package goal)

;; name: menu2-COMMON-GAME.gc
;; name in dgo: menu2
;; dgos: COMMON, GAME

;; og:preserve-this issue 699 menu-road rung 3 round 6: this stub's own
;; "COMMON, GAME" home claim does not hold against a fresh dgo.txt listing
;; (scratch decompiler run against this tree's own retail ISO, GAME.CGO
;; and MENU2.DGO sections): the "menu2" code object appears only as
;; menu2-GAME.o (in GAME.CGO, right after menu2-part.o) and menu2-MENU2.o
;; (in MENU2.DGO, as the DGO's final entry), never as menu2-COMMON.o in
;; any COMMON section. The real, landed home of this object is
;; engine/ui/menu2.gc, header corrected to "dgos: GAME, MENU2" to match;
;; it is linked into game.gd only (GAME.CGO's persistent copy), the same
;; pattern menu2-h.gc/menu2-lists.gc already use for their own retail
;; ENGINE membership that this tree never builds. COMMON.DGO has no .gd
;; in this tree at all, so this stub was never reachable to begin with.
;; Left in place rather than deleted since its decode provenance predates
;; this audit and is not mine to erase without being able to verify it.

;; DECOMP BEGINS

