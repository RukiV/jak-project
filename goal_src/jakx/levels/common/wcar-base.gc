;;-*-Lisp-*-
(in-package goal)

;; name: wcar-base.gc
;; name in dgo: wcar-base
;; dgos: COMMON, GAME

;; DECOMP BEGINS

;; og:preserve-this the brief's own §2.3 called wcar-base "zero own fields,
;; zero own methods", but the capture actually carries two: jmod-shock-tops
;; and jmod-shock-mids, both UNKNOWN 4. Landed as extent-true pads (128B
;; each) rather than the real joint-mod-rotate-local type, same reasoning
;; as wvehicle's jmod-steering-wheel (goal_src/jakx/engine/vehicle/
;; wvehicle-h.gc): joint-mod-base is not landed anywhere in goal_src yet,
;; and the span fits exactly (4 x 32B = 128B, matching
;; joint-mod-rotate-local's own size, for both fields). A 12-byte gap
;; precedes them (wvehicle ends at 4660, jmod-shock-tops starts at 4672)
;; and a 128-byte gap follows them (jmod-shock-mids ends at 4928, the
;; asserted size is 5,056); neither is resolvable from this rung's scoped
;; decode (wcar-base.o was not part of the allowed_objects list, and
;; wcar-skel/wcar are both "never captured" per the brief's §2.2, empty
;; banner sections with no inventory to scope from).
(deftype wcar-base (wvehicle)
  ((pad-post-wvehicle uint8 12  :offset-assert 4660)
   (jmod-shock-tops   uint8 128 :offset-assert 4672)
   (jmod-shock-mids   uint8 128 :offset-assert 4800)
   (pad-tail          uint8 128 :offset-assert 4928)
   )
  :method-count-assert 272
  :size-assert         #x13c0
  :flag-assert         #x110133013c0
  )

;; og:preserve-this vehicle rung (R9): decodes clean today (zero error
;; markers, a scoped decode of this worktree's own state), landed verbatim.
;; method 115 checked the same way and held: it still fails type prop at op
;; 116 ("add failed: <uninitialized> <integer 176>") and cannot guess stack
;; use at op 62, unresolved by this rung.
;; definition for method 65 of type wcar-base
;; WARN: Return type mismatch int vs none.
(defmethod rigid-body-object-method-65 ((this wcar-base))
  (let ((s5-0 (new 'process 'collide-shape-moving this (collide-list-enum usually-hit-by-player))))
    (set! (-> s5-0 dynam) (copy *standard-dynamics* 'process))
    (set! (-> s5-0 reaction) cshape-reaction-default)
    (set! (-> s5-0 no-reaction)
          (the-as (function collide-shape-moving collide-query vector vector object) nothing)
          )
    (set! (-> s5-0 penetrate-using) (penetrate vehicle))
    (let ((s4-0 (new 'process 'collide-shape-prim-group s5-0 (the-as uint 7) 0)))
      (set! (-> s5-0 total-prims) (the-as uint 8))
      (set! (-> s4-0 prim-core action) (collide-action solid))
      (set! (-> s5-0 root-prim) s4-0)
      )
    (let ((a0-5 (new 'process 'collide-shape-prim-sphere s5-0 (the-as uint 321))))
      (set! (-> a0-5 prim-core action) (collide-action solid))
      (set! (-> a0-5 transform-index) 0)
      )
    (let ((a0-7 (new 'process 'collide-shape-prim-sphere s5-0 (the-as uint 322))))
      (set! (-> a0-7 prim-core action) (collide-action solid))
      (set! (-> a0-7 transform-index) 0)
      )
    (let ((a0-9 (new 'process 'collide-shape-prim-sphere s5-0 (the-as uint 596))))
      (set! (-> a0-9 prim-core action) (collide-action solid))
      (set! (-> a0-9 transform-index) 0)
      )
    (let ((a0-11 (new 'process 'collide-shape-prim-sphere s5-0 (the-as uint 616))))
      (set! (-> a0-11 prim-core action) (collide-action solid))
      (set! (-> a0-11 transform-index) 0)
      )
    (let ((a0-13 (new 'process 'collide-shape-prim-sphere s5-0 (the-as uint 323))))
      (set! (-> a0-13 prim-core action) (collide-action solid))
      (set! (-> a0-13 transform-index) 0)
      )
    (let ((v1-20 (new 'process 'collide-shape-prim-sphere s5-0 (the-as uint 1919))))
      (set! (-> v1-20 prim-core action) (collide-action solid nav-sphere))
      (set! (-> v1-20 transform-index) 0)
      (set! (-> v1-20 nav-radius) 20480.0)
      )
    (let ((v1-22 (new 'process 'collide-shape-prim-sphere s5-0 (the-as uint 588))))
      (set! (-> v1-22 prim-core action) (collide-action solid nav-sphere))
      (set! (-> v1-22 transform-index) 0)
      (set! (-> v1-22 nav-radius) 20480.0)
      )
    (set! (-> s5-0 nav-radius) 20480.0)
    (let ((v1-24 (-> s5-0 root-prim)))
      (set! (-> s5-0 backup-collide-as) (-> v1-24 prim-core collide-as))
      (set! (-> s5-0 backup-collide-with) (-> v1-24 prim-core collide-with))
      )
    (set! (-> this root) s5-0)
    )
  0
  (none)
  )

