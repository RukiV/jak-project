;;-*-Lisp-*-
(in-package goal)

;; name: haven-obs.gc
;; name in dgo: haven-obs
;; dgos: HAVENW, HAVJUNGW, HAVSEWW, HAVTOURW, HJX, HSX, HVX

;; DECOMP BEGINS

;; definition of type haven-gen-light
(deftype haven-gen-light (net-simple-destruct)
  ()
  )

;; definition for method 3 of type haven-gen-light
(defmethod inspect ((this haven-gen-light))
  (when (not this)
    (set! this this)
    (goto cfg-4)
    )
  (let ((t9-0 (method-of-type net-simple-destruct inspect)))
    (t9-0 this)
    )
  (label cfg-4)
  this
  )

;; failed to figure out what this is:
(defskelgroup skel-haven-gen-light haven-gen-light haven-gen-light-lod0-jg haven-gen-light-idle-ja
              ((haven-gen-light-lod0-mg (meters 999999)))
              :bounds (static-spherem 0 10 0 15)
              :origin-joint-index 3
              )

;; failed to figure out what this is:
(defskelgroup skel-haven-gen-light-debris-a haven-gen-light-debris haven-gen-light-debris-a-lod0-jg -1
              ((haven-gen-light-debris-a-lod0-mg (meters 999999)))
              :bounds (static-spherem 0 0 0 5)
              )

;; failed to figure out what this is:
(defskelgroup skel-haven-gen-light-debris-b haven-gen-light-debris haven-gen-light-debris-b-lod0-jg -1
              ((haven-gen-light-debris-b-lod0-mg (meters 999999)))
              :bounds (static-spherem 0 0 0 5)
              )

;; failed to figure out what this is:
(defskelgroup skel-haven-gen-light-debris-c haven-gen-light-debris haven-gen-light-debris-c-lod0-jg -1
              ((haven-gen-light-debris-c-lod0-mg (meters 999999)))
              :bounds (static-spherem 0 0 0 5)
              )

;; failed to figure out what this is:
(defskelgroup skel-haven-gen-light-debris-d haven-gen-light-debris haven-gen-light-debris-d-lod0-jg -1
              ((haven-gen-light-debris-d-lod0-mg (meters 999999)))
              :bounds (static-spherem 0 0 0 5)
              )

;; definition for symbol *haven-gen-light-debris-params*, type object
(define *haven-gen-light-debris-params*
  (the-as object (new 'static 'debris-static-params
                   :joints (new 'static 'boxed-array :type debris-static-joint-params
                     (new 'static 'debris-static-joint-params
                       :parent-joint-index 3
                       :group "skel-haven-gen-light-debris-a"
                       :offset (new 'static 'vector :data (new 'static 'array float 4 4096.0 8192.0 4096.0 1.0))
                       )
                     (new 'static 'debris-static-joint-params
                       :parent-joint-index 3
                       :group "skel-haven-gen-light-debris-b"
                       :offset (new 'static 'vector :data (new 'static 'array float 4 -4096.0 16384.0 0.0 1.0))
                       )
                     (new 'static 'debris-static-joint-params
                       :parent-joint-index 3
                       :group "skel-haven-gen-light-debris-a"
                       :offset (new 'static 'vector :data (new 'static 'array float 4 4096.0 24576.0 12288.0 1.0))
                       )
                     (new 'static 'debris-static-joint-params
                       :parent-joint-index 3
                       :group "skel-haven-gen-light-debris-b"
                       :offset (new 'static 'vector :data (new 'static 'array float 4 -4096.0 32768.0 0.0 1.0))
                       )
                     (new 'static 'debris-static-joint-params
                       :parent-joint-index 3
                       :group "skel-haven-gen-light-debris-b"
                       :offset (new 'static 'vector :data (new 'static 'array float 4 0.0 49152.0 0.0 1.0))
                       )
                     (new 'static 'debris-static-joint-params
                       :parent-joint-index 3
                       :group "skel-haven-gen-light-debris-c"
                       :offset (new 'static 'vector :data (new 'static 'array float 4 0.0 57344.0 0.0 1.0))
                       )
                     (new 'static 'debris-static-joint-params
                       :parent-joint-index 3
                       :group "skel-haven-gen-light-debris-c"
                       :offset (new 'static 'vector :data (new 'static 'array float 4 0.0 81920.0 0.0 1.0))
                       )
                     (new 'static 'debris-static-joint-params
                       :parent-joint-index 3
                       :group "skel-haven-gen-light-debris-c"
                       :offset (new 'static 'vector :data (new 'static 'array float 4 0.0 106496.0 0.0 1.0))
                       )
                     (new 'static 'debris-static-joint-params
                       :parent-joint-index 3
                       :group "skel-haven-gen-light-debris-d"
                       :offset (new 'static 'vector :data (new 'static 'array float 4 0.0 122880.0 0.0 1.0))
                       )
                     (new 'static 'debris-static-joint-params
                       :parent-joint-index 3
                       :group "skel-haven-gen-light-debris-d"
                       :offset (new 'static 'vector :data (new 'static 'array float 4 8192.0 122880.0 12288.0 1.0))
                       )
                     )
                   :collide-spec (collide-spec backgnd)
                   :sound-hit #x263
                   :initial-sound-hit #x264
                   :art-level #f
                   :vscale 20480.0
                   :volume-time 3.0
                   :play-delay 45
                   :min-speed 32768.0
                   :max-speed 73728.0
                   )
                 )
  )

;; definition for method 66 of type haven-gen-light
(defmethod destroy! ((this haven-gen-light))
  (rlet ((vf0 :class vf)
         (vf4 :class vf)
         (vf5 :class vf)
         (vf6 :class vf)
         )
    (init-vf0-vector)
    (let ((v1-1 (make-sound-instance (-> *sound-info* 613) (new 'static 'sound-id) (the-as handle #f))))
      (if v1-1
          (-> v1-1 id)
          0
          )
      )
    (set! (-> *part-tracker-params-default* group) (-> *part-group-id-table* 385))
    (vector-reset! (-> *part-tracker-params-default* local-offset))
    (set! (-> *part-tracker-params-default* duration) (the-as uint 1500))
    (set! (-> *part-tracker-params-default* callback) #f)
    (set! (-> *part-tracker-params-default* userdata) (the-as uint #f))
    (set! (-> *part-tracker-params-default* target) #f)
    (set! (-> *part-tracker-params-default* mat-joint) (-> this node-list data 3 bone transform))
    (set! (-> *part-tracker-params-default* local-space-param) 0)
    (set! (-> *part-tracker-params-default* local-space-callback) #f)
    (set! (-> *part-tracker-params-default* subsampler-num) 1.0)
    *part-tracker-params-default*
    (if (logtest? (-> *part-group-id-table* 385 flags) (sp-group-flag sp13))
        (process-spawn
          part-tracker-subsampler
          :init part-tracker-subsampler-init
          *part-tracker-params-default*
          :name "part-tracker-subsampler"
          :to *entity-pool*
          :unk 0
          )
        (process-spawn
          part-tracker
          :init part-tracker-init
          *part-tracker-params-default*
          :name "part-tracker"
          :to *entity-pool*
          :unk 0
          )
        )
    (let ((s5-2 (new 'stack 'debris-tuning (the-as uint 0))))
      (set-vector! (-> s5-2 fountain-rand-transv-lo) -40960.0 61440.0 -40960.0 1.0)
      (let ((a1-8 (-> s5-2 fountain-rand-transv-lo)))
        (let ((v1-34 (-> s5-2 fountain-rand-transv-lo))
              (a0-16 (-> this impact-velocity))
              )
          (.lvf vf4 (&-> v1-34 quad))
          (.lvf vf5 (&-> a0-16 quad))
          )
        (.mov.vf.w vf6 vf0)
        (.add.vf.xyz vf6 vf4 vf5)
        (.svf (&-> a1-8 quad) vf6)
        )
      (set-vector! (-> s5-2 fountain-rand-transv-hi) 40960.0 61440.0 40960.0 1.0)
      (let ((a1-9 (-> s5-2 fountain-rand-transv-hi)))
        (let ((v1-36 (-> s5-2 fountain-rand-transv-hi))
              (a0-21 (-> this impact-velocity))
              )
          (.lvf vf4 (&-> v1-36 quad))
          (.lvf vf5 (&-> a0-21 quad))
          )
        (.mov.vf.w vf6 vf0)
        (.add.vf.xyz vf6 vf4 vf5)
        (.svf (&-> a1-9 quad) vf6)
        )
      (set! (-> s5-2 fountain-rand-transv-hi w) 16384.0)
      (set! (-> s5-2 duration) (seconds 2))
      (set! (-> s5-2 scale-rand-lo) 1.0)
      (set! (-> s5-2 scale-rand-hi) 1.0)
      (when (type? (-> this root) collide-shape)
        (let ((v1-43 (-> this root root-prim)))
          (set! (-> v1-43 prim-core collide-as) (collide-spec))
          (set! (-> v1-43 prim-core collide-with) (collide-spec))
          )
        0
        )
      (logior! (-> this draw status) (draw-control-status no-draw))
      (if (type? (-> this root) collide-shape)
          (transform-post)
          )
      (debris-spawn
        this
        s5-2
        (the-as debris-static-params *haven-gen-light-debris-params*)
        (the-as process-drawable #f)
        )
      )
    ((method-of-type net-simple-destruct destroy!) this)
    (none)
    )
  )

;; definition for method 11 of type haven-gen-light
(defmethod init-from-entity! ((this haven-gen-light) (arg0 entity-actor))
  (stack-size-set! (-> this main-thread) 32)
  (logior! (-> this mask) (process-mask actor-pause))
  (let ((s4-0 (new 'process 'collide-shape this (collide-list-enum usually-hit-by-player))))
    (let ((s3-0 (new 'process 'collide-shape-prim-group s4-0 (the-as uint 1) 0)))
      (set! (-> s4-0 total-prims) (the-as uint 2))
      (set! (-> s3-0 prim-core collide-as) (collide-spec obstacle camera-blocker pusher))
      (set! (-> s3-0 prim-core collide-with) (collide-spec jak bot player-list))
      (set! (-> s3-0 prim-core action) (collide-action solid))
      (set! (-> s3-0 transform-index) 3)
      (set-vector! (-> s3-0 local-sphere) 0.0 40960.0 0.0 61440.0)
      (set! (-> s4-0 root-prim) s3-0)
      )
    (pusher-init s4-0)
    (let ((v1-15 (new 'process 'collide-shape-prim-mesh s4-0 (the-as uint 0) (the-as uint 0))))
      (set! (-> v1-15 prim-core collide-as) (collide-spec obstacle camera-blocker pusher))
      (set! (-> v1-15 prim-core collide-with) (collide-spec jak bot player-list))
      (set! (-> v1-15 prim-core action) (collide-action solid))
      (set! (-> v1-15 transform-index) 3)
      (set-vector! (-> v1-15 local-sphere) 0.0 40960.0 0.0 61440.0)
      )
    (set! (-> s4-0 nav-radius) (* 0.75 (-> s4-0 root-prim local-sphere w)))
    (let ((v1-18 (-> s4-0 root-prim)))
      (set! (-> s4-0 backup-collide-as) (-> v1-18 prim-core collide-as))
      (set! (-> s4-0 backup-collide-with) (-> v1-18 prim-core collide-with))
      )
    (set! (-> this root) s4-0)
    )
  (initialize-skeleton
    this
    (the-as skeleton-group (art-group-get-by-name *level* "skel-haven-gen-light" (the-as (pointer level) #f)))
    (the-as pair 0)
    )
  (set! (-> this hit-points) 10.0)
  (process-drawable-from-entity! this arg0)
  (set! (-> this draw light-index) (the-as uint 10))
  (go (method-of-object this idle))
  )

;; definition of type haven-trashcan
(deftype haven-trashcan (construction-object)
  ((has-hit-ground  basic)
   (barrel-roll     uint32)
   (rolling         basic)
   )
  )

;; definition for method 3 of type haven-trashcan
(defmethod inspect ((this haven-trashcan))
  (when (not this)
    (set! this this)
    (goto cfg-4)
    )
  (let ((t9-0 (method-of-type construction-object inspect)))
    (t9-0 this)
    )
  (format #t "~2Thas-hit-ground: ~A~%" (-> this has-hit-ground))
  (format #t "~2Tbarrel-roll: ~D~%" (-> this barrel-roll))
  (format #t "~2Trolling: ~A~%" (-> this rolling))
  (label cfg-4)
  this
  )

;; failed to figure out what this is:
(defskelgroup skel-haven-trashcan haven-trashcan haven-trashcan-lod0-jg haven-trashcan-idle-ja
              ((haven-trashcan-lod0-mg (meters 999999)))
              :bounds (static-spherem 0 2.5 0 3.5)
              :origin-joint-index 3
              )

;; definition for symbol *haven-trashcan-constants*, type rigid-body-object-constants
(define *haven-trashcan-constants*
  (new 'static 'rigid-body-object-constants
    :info (new 'static 'rigid-body-info
      :max-time-step 0.016666668
      :mass 0.001
      :inv-mass 999.99994
      :angular-damping 0.03
      :bounce-factor 0.7
      :friction-factor 0.9
      :cm-offset-joint (new 'static 'vector :data (new 'static 'array float 4 0.0 12288.0 0.0 1.0))
      :inertial-tensor-box (new 'static 'array meters 3 (meters 3) (meters 5) (meters 3))
      :gravity 409600.0
      )
    :name '*haven-trashcan-constants*
    )
  )

;; definition for method 80 of type haven-trashcan
(defmethod rigid-body-object-method-80 ((this haven-trashcan) (arg0 rigid-body-impact))
  (let ((f30-0 (* (lerp-scale 0.5 1.0 (vector-length (-> this root transv)) 0.0 40960.0)
                  (fmin 1.0 (+ 0.25 (* 0.012207031 (-> arg0 impulse))))
                  )
               )
        )
    (cond
      ((and (-> arg0 process) (logtest? (process-mask vehicle) (-> arg0 process mask)) (< 28.672 (-> arg0 impulse)))
       (let ((v1-11 (make-sound-instance (-> *sound-info* 576) (new 'static 'sound-id) (the-as handle #f))))
         (cond
           (v1-11
             (set! (-> v1-11 volume) (the int (* 1024.0 f30-0)))
             (logior! (-> v1-11 flags) 64)
             (-> v1-11 id)
             )
           (else
             0
             )
           )
         )
       (set! (-> this has-hit-ground) #f)
       )
      ((not (-> this has-hit-ground))
       (set! (-> this has-hit-ground) (the-as basic #t))
       (let ((v1-18 (make-sound-instance (-> *sound-info* 577) (new 'static 'sound-id) (the-as handle #f))))
         (cond
           (v1-18
             (set! (-> v1-18 volume) (the int (* 1024.0 f30-0)))
             (logior! (-> v1-18 flags) 64)
             (-> v1-18 id)
             )
           (else
             0
             )
           )
         )
       )
      (else
        (let ((v1-22 (make-sound-instance (-> *sound-info* 578) (new 'static 'sound-id) (the-as handle #f))))
          (cond
            (v1-22
              (set! (-> v1-22 volume) (the int (* 1024.0 f30-0)))
              (logior! (-> v1-22 flags) 64)
              (-> v1-22 id)
              )
            (else
              0
              )
            )
          )
        )
      )
    )
  ((method-of-type construction-object rigid-body-object-method-80) this arg0)
  (none)
  )

;; definition for method 89 of type haven-trashcan
;; INFO: Used lq/sq
;; WARN: Return type mismatch int vs none.
(defmethod construction-object-method-89 ((this haven-trashcan))
  (local-vars (v1-9 float) (v1-14 float))
  (rlet ((acc :class vf)
         (vf0 :class vf)
         (vf1 :class vf)
         (vf2 :class vf)
         )
    (init-vf0-vector)
    (when (>= (the-as uint (- (current-time) (the-as int (-> this disturbed-time)))) (the-as uint 600))
      (let ((f0-1 (* (vector-vector-distance-squared (-> this root trans) (camera-pos)) (square 0.00012207031))))
        (.lvf vf1 (&-> (-> this rbody ang-velocity) quad))
        (.add.w.vf.x vf2 vf0 vf0)
        (.mul.vf vf1 vf1 vf1)
        (.mul.x.vf.x acc vf2 vf1)
        (.add.mul.y.vf.x acc vf2 vf1 acc)
        (.add.mul.z.vf.x vf1 vf2 vf1 acc)
        (.mov v1-9 vf1)
        (cond
          ((and (< v1-9 f0-1) (begin
                                (.lvf vf1 (&-> (-> this rbody lin-velocity) quad))
                                (.add.w.vf.x vf2 vf0 vf0)
                                (.mul.vf vf1 vf1 vf1)
                                (.mul.x.vf.x acc vf2 vf1)
                                (.add.mul.y.vf.x acc vf2 vf1 acc)
                                (.add.mul.z.vf.x vf1 vf2 vf1 acc)
                                (.mov v1-14 vf1)
                                (< v1-14 (* f0-1 (square 614.4)))
                                )
                )
           (set! (-> this rbody lin-momentum quad) (the-as uint128 0))
           (set! (-> this rbody ang-momentum quad) (the-as uint128 0))
           (construction-object-method-91 this #f)
           (sound-stop (the-as sound-id (-> this barrel-roll)) #t)
           (set! (-> this barrel-roll) (the-as uint 0))
           (set! (-> this rolling) #f)
           (go (method-of-object this idle))
           )
          (else
            (set! (-> this disturbed-time) (the-as uint (current-time)))
            )
          )
        )
      )
    0
    (none)
    )
  )

;; definition for method 65 of type haven-trashcan
;; WARN: Return type mismatch int vs none.
(defmethod rigid-body-object-method-65 ((this haven-trashcan))
  (set! (-> this has-hit-ground) #f)
  (let ((s5-0 (new 'process 'collide-shape-moving this (collide-list-enum usually-hit-by-player))))
    (set! (-> s5-0 dynam) (copy *standard-dynamics* 'process))
    (set! (-> s5-0 reaction) cshape-reaction-default)
    (set! (-> s5-0 no-reaction)
          (the-as (function collide-shape-moving collide-query vector vector object) nothing)
          )
    (let ((s4-0 (new 'process 'collide-shape-prim-group s5-0 (the-as uint 2) 0)))
      (set! (-> s5-0 total-prims) (the-as uint 3))
      (set! (-> s4-0 prim-core collide-as) (collide-spec obstacle pusher))
      (set! (-> s4-0 prim-core collide-with) (collide-spec
                                               backgnd
                                               bot
                                               crate
                                               civilian
                                               enemy
                                               obstacle
                                               vehicle-sphere
                                               hit-by-player-list
                                               hit-by-others-list
                                               pusher
                                               shield
                                               vehicle-sphere-no-probe
                                               )
            )
      (set! (-> s4-0 prim-core action) (collide-action solid))
      (set! (-> s4-0 transform-index) 3)
      (set-vector! (-> s4-0 local-sphere) 0.0 10240.0 0.0 14336.0)
      (set! (-> s5-0 root-prim) s4-0)
      )
    (pusher-init s5-0)
    (let ((v1-15 (new 'process 'collide-shape-prim-sphere s5-0 (the-as uint 0))))
      (set! (-> v1-15 prim-core collide-as) (collide-spec obstacle pusher))
      (set! (-> v1-15 prim-core collide-with) (collide-spec
                                                backgnd
                                                bot
                                                crate
                                                civilian
                                                enemy
                                                obstacle
                                                vehicle-sphere
                                                hit-by-player-list
                                                hit-by-others-list
                                                pusher
                                                shield
                                                vehicle-sphere-no-probe
                                                )
            )
      (set! (-> v1-15 prim-core action) (collide-action solid))
      (set! (-> v1-15 transform-index) 3)
      (set-vector! (-> v1-15 local-sphere) 0.0 4096.0 0.0 8192.0)
      )
    (let ((v1-17 (new 'process 'collide-shape-prim-sphere s5-0 (the-as uint 0))))
      (set! (-> v1-17 prim-core collide-as) (collide-spec obstacle pusher))
      (set! (-> v1-17 prim-core collide-with) (collide-spec
                                                backgnd
                                                bot
                                                crate
                                                civilian
                                                enemy
                                                obstacle
                                                vehicle-sphere
                                                hit-by-player-list
                                                hit-by-others-list
                                                pusher
                                                shield
                                                vehicle-sphere-no-probe
                                                )
            )
      (set! (-> v1-17 prim-core action) (collide-action solid))
      (set! (-> v1-17 transform-index) 3)
      (set-vector! (-> v1-17 local-sphere) 0.0 16384.0 0.0 8192.0)
      )
    (set! (-> s5-0 nav-radius) (* 0.75 (-> s5-0 root-prim local-sphere w)))
    (let ((v1-20 (-> s5-0 root-prim)))
      (set! (-> s5-0 backup-collide-as) (-> v1-20 prim-core collide-as))
      (set! (-> s5-0 backup-collide-with) (-> v1-20 prim-core collide-with))
      )
    (set! (-> s5-0 event-self) 'touched)
    (set! (-> this root) s5-0)
    )
  0
  (none)
  )

;; definition for method 66 of type haven-trashcan
;; WARN: Return type mismatch int vs none.
(defmethod rigid-body-object-method-66 ((this haven-trashcan))
  (construction-object-method-90 this)
  (initialize-skeleton
    this
    (the-as skeleton-group (art-group-get-by-name *level* "skel-haven-trashcan" (the-as (pointer level) #f)))
    (the-as pair 0)
    )
  (rigid-body-object-method-64 this *haven-trashcan-constants*)
  (logior! (-> this rbody flags) 16)
  (set! (-> this draw light-index) (the-as uint 10))
  (set! (-> this rolling) #f)
  (set! (-> this barrel-roll) (the-as uint 0))
  0
  (none)
  )




