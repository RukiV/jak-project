;;-*-Lisp-*-
(in-package goal)

;; name: kras-obs.gc
;; name in dgo: kras-obs
;; dgos: DKKX, DOCKKRAW, KRASW, KRATOURW, KRX

;; DECOMP BEGINS

;;-*-Lisp-*-
(in-package goal)

;; definition of type kras-tower
(deftype kras-tower (process-drawable)
  ()
  (:state-methods
    idle
    )
  )

;; definition for method 3 of type kras-tower
(defmethod inspect ((this kras-tower))
  (when (not this)
    (set! this this)
    (goto cfg-4)
    )
  (let ((t9-0 (method-of-type process-drawable inspect)))
    (t9-0 this)
    )
  (label cfg-4)
  this
  )

;; failed to figure out what this is:
(defskelgroup skel-kras-tower kras-tower kras-tower-lod0-jg kras-tower-idle-ja
              ((kras-tower-lod0-mg (meters 999999)))
              :bounds (static-spherem 0 0 0 250)
              :origin-joint-index 3
              )

;; definition for method 11 of type kras-tower
(defmethod init-from-entity! ((this kras-tower) (arg0 entity-actor))
  (stack-size-set! (-> this main-thread) 32)
  (set! (-> this root) (new 'process 'trsqv))
  (process-drawable-from-entity! this arg0)
  (initialize-skeleton
    this
    (the-as skeleton-group (art-group-get-by-name *level* "skel-kras-tower" (the-as (pointer level) #f)))
    (the-as pair 0)
    )
  (ja-post)
  (go (method-of-object this idle))
  )

;; failed to figure out what this is:
(defstate idle (kras-tower)
  :virtual #t
  :code sleep-code
  :post (behavior ()
    (quaternion-rotate-y!
      (the-as quaternion (-> self root rot))
      (the-as quaternion (-> self root rot))
      (* 2730.6667 (seconds-per-frame))
      )
    (ja-post)
    )
  )

;; definition of type kras-fishtank-lo
(deftype kras-fishtank-lo (process-drawable)
  ()
  (:state-methods
    idle
    )
  )

;; definition for method 3 of type kras-fishtank-lo
(defmethod inspect ((this kras-fishtank-lo))
  (when (not this)
    (set! this this)
    (goto cfg-4)
    )
  (let ((t9-0 (method-of-type process-drawable inspect)))
    (t9-0 this)
    )
  (label cfg-4)
  this
  )

;; failed to figure out what this is:
(defskelgroup skel-kras-fishtank-lo kras-fishtank-lo kras-fishtank-lo-lod0-jg kras-fishtank-lo-idle-ja
              ((kras-fishtank-lo-lod0-mg (meters 999999)))
              :bounds (static-spherem 0 5 0 15)
              :origin-joint-index 3
              )

;; definition for method 11 of type kras-fishtank-lo
(defmethod init-from-entity! ((this kras-fishtank-lo) (arg0 entity-actor))
  (stack-size-set! (-> this main-thread) 32)
  (set! (-> this root) (new 'process 'trsqv))
  (process-drawable-from-entity! this arg0)
  (initialize-skeleton
    this
    (the-as skeleton-group (art-group-get-by-name *level* "skel-kras-fishtank-lo" (the-as (pointer level) #f)))
    (the-as pair 0)
    )
  (ja-post)
  (go (method-of-object this idle))
  )

;; failed to figure out what this is:
(defstate idle (kras-fishtank-lo)
  :virtual #t
  :code sleep-code
  )

;; definition of type kras-rusty-sign
(deftype kras-rusty-sign (process-drawable)
  ()
  (:state-methods
    idle
    )
  )

;; definition for method 3 of type kras-rusty-sign
(defmethod inspect ((this kras-rusty-sign))
  (when (not this)
    (set! this this)
    (goto cfg-4)
    )
  (let ((t9-0 (method-of-type process-drawable inspect)))
    (t9-0 this)
    )
  (label cfg-4)
  this
  )

;; failed to figure out what this is:
(defskelgroup skel-kras-rusty-sign kras-rusty-sign kras-rusty-sign-lod0-jg kras-rusty-sign-idle-ja
              ((kras-rusty-sign-lod0-mg (meters 999999)))
              :bounds (static-spherem -5 -2 0 7)
              :origin-joint-index 3
              )

;; definition for method 11 of type kras-rusty-sign
(defmethod init-from-entity! ((this kras-rusty-sign) (arg0 entity-actor))
  (stack-size-set! (-> this main-thread) 32)
  (set! (-> this root) (new 'process 'trsqv))
  (process-drawable-from-entity! this arg0)
  (initialize-skeleton
    this
    (the-as skeleton-group (art-group-get-by-name *level* "skel-kras-rusty-sign" (the-as (pointer level) #f)))
    (the-as pair 0)
    )
  (ja-post)
  (go (method-of-object this idle))
  )

;; failed to figure out what this is:
(defstate idle (kras-rusty-sign)
  :virtual #t
  :code (behavior ()
    (until #f
      (ja-no-eval :group! (ja-group) :num! (seek!) :frame-num 0.0)
      (until (ja-done? 0)
        (suspend)
        (ja :num! (seek!))
        )
      )
    #f
    )
  :post ja-post
  )

;; definition of type kras-fireworks-ship-a
(deftype kras-fireworks-ship-a (process-drawable)
  ((curve      basic)
   (location   float)
   (fireworks  uint64)
   )
  (:state-methods
    idle
    )
  )

;; definition for method 3 of type kras-fireworks-ship-a
(defmethod inspect ((this kras-fireworks-ship-a))
  (when (not this)
    (set! this this)
    (goto cfg-4)
    )
  (let ((t9-0 (method-of-type process-drawable inspect)))
    (t9-0 this)
    )
  (format #t "~2Tcurve: ~A~%" (-> this curve))
  (format #t "~2Tlocation: ~f~%" (-> this location))
  (format #t "~2Tfireworks: ~D~%" (-> this fireworks))
  (label cfg-4)
  this
  )

;; failed to figure out what this is:
(defskelgroup skel-kras-fireworks-ship-a kras-fireworks-ship-a kras-fireworks-ship-a-lod0-jg kras-fireworks-ship-a-idle-ja
              ((kras-fireworks-ship-a-lod0-mg (meters 999999)))
              :bounds (static-spherem 0 0 0 60)
              :origin-joint-index 3
              )

;; definition for method 11 of type kras-fireworks-ship-a
(defmethod init-from-entity! ((this kras-fireworks-ship-a) (arg0 entity-actor))
  (stack-size-set! (-> this main-thread) 32)
  (set! (-> this level) (level-group-method-32 *level*))
  (set! (-> this root) (new 'process 'trsqv))
  (set! (-> this curve) (new 'process 'curve-control this 'path (the-as res-lump #f) #f -1000000000.0))
  (process-drawable-from-entity! this arg0)
  (initialize-skeleton
    this
    (the-as
      skeleton-group
      (art-group-get-by-name *level* "skel-kras-fireworks-ship-a" (the-as (pointer level) #f))
      )
    (the-as pair 0)
    )
  (set! (-> this part) (create-launch-control (-> *part-group-id-table* 393) this))
  (go (method-of-object this idle))
  )

;; definition for method 7 of type kras-fireworks-ship-a
;; WARN: Return type mismatch process-drawable vs kras-fireworks-ship-a.
(defmethod relocate ((this kras-fireworks-ship-a) (offset int))
  (if (nonzero? (-> this curve))
      (&+! (-> this curve) offset)
      )
  (the-as kras-fireworks-ship-a ((method-of-type process-drawable relocate) this offset))
  )

;; failed to figure out what this is:
(defstate idle (kras-fireworks-ship-a)
  :virtual #t
  :enter (behavior ()
    (set! (-> self location) 0.0)
    (set! (-> *launch-matrix* trans quad) (-> *null-vector* quad))
    (set! (-> *part-tracker-params-default* group) (-> *part-group-id-table* 393))
    (vector-reset! (-> *part-tracker-params-default* local-offset))
    (set! (-> *part-tracker-params-default* duration) (the-as uint -1))
    (set! (-> *part-tracker-params-default* callback) #f)
    (set! (-> *part-tracker-params-default* userdata) (the-as uint #f))
    (set! (-> *part-tracker-params-default* target) self)
    (set! (-> *part-tracker-params-default* mat-joint) (the-as object 0))
    (set! (-> *part-tracker-params-default* local-space-param) 0)
    (set! (-> *part-tracker-params-default* local-space-callback) #f)
    (set! (-> *part-tracker-params-default* subsampler-num) 1.0)
    *part-tracker-params-default*
    (set! (-> self fireworks)
          (the-as uint (ppointer->handle (if (logtest? (-> *part-group-id-table* 393 flags) (sp-group-flag sp13))
                                             (process-spawn
                                               part-tracker-subsampler
                                               :init part-tracker-subsampler-init
                                               *part-tracker-params-default*
                                               :name "part-tracker-subsampler"
                                               :to *entity-pool*
                                               :unk 0
                                               )
                                             (process-spawn
                                               part-tracker
                                               :init part-tracker-init
                                               *part-tracker-params-default*
                                               :name "part-tracker"
                                               :to *entity-pool*
                                               :unk 0
                                               )
                                             )
                                         )
                  )
          )
    )
  ;; og:preserve-this: the retail :exit and :post handlers both decoded as
  ;; raw labels (L122, L114), anon-functions the decompiler never
  ;; recognized as proper functions; raw label references are not valid
  ;; GOAL, so both keys are omitted entirely, matching the credits-obs
  ;; precedent for the same defect class.
  :code sleep-code
  )

;; definition of type kras-flag
(deftype kras-flag (process-drawable)
  ()
  (:state-methods
    idle
    )
  )

;; definition for method 3 of type kras-flag
(defmethod inspect ((this kras-flag))
  (when (not this)
    (set! this this)
    (goto cfg-4)
    )
  (let ((t9-0 (method-of-type process-drawable inspect)))
    (t9-0 this)
    )
  (label cfg-4)
  this
  )

;; failed to figure out what this is:
(defskelgroup skel-kras-flag kras-flag kras-flag-lod0-jg kras-flag-idle-ja
              ((kras-flag-lod0-mg (meters 999999)))
              :bounds (static-spherem 0 5 -17 23)
              :origin-joint-index 3
              )

;; definition for method 11 of type kras-flag
(defmethod init-from-entity! ((this kras-flag) (arg0 entity-actor))
  (stack-size-set! (-> this main-thread) 32)
  (set! (-> this root) (new 'process 'trsqv))
  (process-drawable-from-entity! this arg0)
  (initialize-skeleton
    this
    (the-as skeleton-group (art-group-get-by-name *level* "skel-kras-flag" (the-as (pointer level) #f)))
    (the-as pair 0)
    )
  (go (method-of-object this idle))
  )

;; failed to figure out what this is:
(defstate idle (kras-flag)
  :virtual #t
  ;; og:preserve-this: the retail :code handler decoded as a raw label
  ;; (L107), same defect class as this file's other omitted raw-label
  ;; state slots.
  :post ja-post
  )

;; definition of type kras-windsock
(deftype kras-windsock (process-drawable)
  ()
  (:state-methods
    idle
    )
  )

;; definition for method 3 of type kras-windsock
(defmethod inspect ((this kras-windsock))
  (when (not this)
    (set! this this)
    (goto cfg-4)
    )
  (let ((t9-0 (method-of-type process-drawable inspect)))
    (t9-0 this)
    )
  (label cfg-4)
  this
  )

;; failed to figure out what this is:
(defskelgroup skel-kras-windsock kras-windsock kras-windsock-lod0-jg kras-windsock-idle-ja
              ((kras-windsock-lod0-mg (meters 999999)))
              :bounds (static-spherem 0 0 0 35)
              :origin-joint-index 3
              )

;; definition for method 11 of type kras-windsock
(defmethod init-from-entity! ((this kras-windsock) (arg0 entity-actor))
  (stack-size-set! (-> this main-thread) 32)
  (set! (-> this root) (new 'process 'trsqv))
  (process-drawable-from-entity! this arg0)
  (initialize-skeleton
    this
    (the-as skeleton-group (art-group-get-by-name *level* "skel-kras-windsock" (the-as (pointer level) #f)))
    (the-as pair 0)
    )
  (go (method-of-object this idle))
  )

;; failed to figure out what this is:
(defstate idle (kras-windsock)
  :virtual #t
  ;; og:preserve-this: the retail :code handler decoded as a raw label
  ;; (L100), same defect class as this file's other omitted raw-label
  ;; state slots.
  :post ja-post
  )

;; definition of type kras-pump-break-proxy
(deftype kras-pump-break-proxy (process-focusable)
  ((actor-group        (pointer int32))
   (actor-group-count  int32)
   (impact-velocity    vector  :inline)
   (hit-points         float)
   (start-exploded     basic)
   (net-parent         handle)
   )
  (:state-methods
    idle
    explode
    )
  (:methods
    (kras-pump-break-proxy-method-61 (_type_ object object) none)
    (kras-pump-break-proxy-method-62 (_type_ object) none)
    (kras-pump-break-proxy-method-63 (_type_ entity-actor) none)
    (kras-pump-break-proxy-method-64 (_type_) none)
    )
  )

;; definition for method 3 of type kras-pump-break-proxy
(defmethod inspect ((this kras-pump-break-proxy))
  (when (not this)
    (set! this this)
    (goto cfg-7)
    )
  (let ((t9-0 (method-of-type process-focusable inspect)))
    (t9-0 this)
    )
  (format #t "~2Tactor-group: #x~X~%" (-> this actor-group))
  (dotimes (s5-0 (-> this actor-group-count))
    (format
      #t
      "~T  [~D]~2Tactor-group: ~`actor-group`P~%"
      s5-0
      (-> (the-as (pointer uint32) (&+ (-> this actor-group) (* s5-0 4))))
      )
    )
  (format #t "~2Tactor-group-count: ~D~%" (-> this actor-group-count))
  (format #t "~2Timpact-velocity: #<vector @ #x~X>~%" (-> this impact-velocity))
  (format #t "~2Thit-points: ~f~%" (-> this hit-points))
  (format #t "~2Tstart-exploded: ~A~%" (-> this start-exploded))
  (format #t "~2Tnet-parent: ~D~%" (-> this net-parent))
  (label cfg-7)
  this
  )

;; failed to figure out what this is:
(defstate idle (kras-pump-break-proxy)
  :virtual #t
  :event (behavior ((proc process) (argc int) (message symbol) (block event-message-block))
    (case message
      (('impact-impulse)
       (let ((a1-1 (-> block param 0)))
         (kras-pump-break-proxy-method-62 self a1-1)
         )
       )
      (('attack)
       (let ((s4-0 (-> block param 1)))
         (get-penetrate-using-from-attack-event (the-as process-drawable proc) block)
         (let* ((a0-7 self)
                (t9-2 (method-of-object a0-7 kras-pump-break-proxy-method-61))
                )
           (-> block param 0)
           (t9-2 a0-7 proc s4-0)
           )
         )
       )
      (('explode)
       (go-virtual explode)
       )
      (('go-die)
       (go empty-state)
       )
      )
    )
  :code sleep-code
  )

;; failed to figure out what this is:
(defstate explode (kras-pump-break-proxy)
  :virtual #t
  :event (behavior ((proc process) (argc int) (message symbol) (block event-message-block))
    (case message
      (('go-die)
       (go empty-state)
       )
      )
    0
    )
  :enter (behavior ()
    (when (not (-> self start-exploded))
      (kras-pump-break-proxy-method-64 self)
      (send-event (handle->process (-> self net-parent)) 'explode)
      )
    )
  :code sleep-code
  )

;; definition for method 62 of type kras-pump-break-proxy
;; ERROR: function was not converted to expressions. Cannot decompile.

;; definition for method 61 of type kras-pump-break-proxy
;; ERROR: function was not converted to expressions. Cannot decompile.

;; definition of type kras-pump-break-draw
(deftype kras-pump-break-draw (kras-pump-break-proxy)
  ((flame-sound  uint32)
   )
  )

;; definition for method 3 of type kras-pump-break-draw
(defmethod inspect ((this kras-pump-break-draw))
  (when (not this)
    (set! this this)
    (goto cfg-4)
    )
  (let ((t9-0 (method-of-type kras-pump-break-proxy inspect)))
    (t9-0 this)
    )
  (format #t "~2Tflame-sound: ~D~%" (-> this flame-sound))
  (label cfg-4)
  this
  )

;; definition for method 57 of type kras-pump-break-draw
(defmethod process-focusable-method-57 ((this kras-pump-break-draw))
  999.99994
  )

;; og:preserve-this compile-only forward declaration: kras-pump-break's own
;; deftype lands further down this file (after kras-pump-break-draw), but
;; this function's arg0 needs the real type visible here.
(declare-type kras-pump-break basic)

;; definition for function kras-pump-break-draw-init-by-other
;; WARN: Return type mismatch object vs symbol.
(defbehavior kras-pump-break-draw-init-by-other kras-pump-break-draw ((arg0 kras-pump-break) (arg1 entity-actor) (arg2 basic))
  (set! (-> self start-exploded) arg2)
  (kras-pump-break-proxy-method-63 self arg1)
  (the-as symbol (if arg2
                     (go-virtual explode)
                     (go-virtual idle)
                     )
          )
  )

;; definition of type kras-pump-break
(deftype kras-pump-break (process-nettable)
  ((exploded  basic)
   (drawable  handle)
   )
  (:state-methods
    idle
    )
  (:methods
    (kras-pump-break-method-43 (_type_) none)
    (kras-pump-break-method-44 (_type_) none)
    ;; return retyped none -> symbol: (code idle kras-pump-break) wraps the
    ;; call in (not ...), a none-typed value cannot be used in a condition.
    (kras-pump-break-method-45 (_type_) symbol)
    )
  )

;; definition for method 3 of type kras-pump-break
(defmethod inspect ((this kras-pump-break))
  (when (not this)
    (set! this this)
    (goto cfg-4)
    )
  (let ((t9-0 (method-of-type process-nettable inspect)))
    (t9-0 this)
    )
  (format #t "~2Texploded: ~A~%" (-> this exploded))
  (format #t "~2Tdrawable: ~D~%" (-> this drawable))
  (label cfg-4)
  this
  )

;; definition for function kras-pump-break-init-remote
;; WARN: Return type mismatch int vs process.
(defbehavior kras-pump-break-init-remote kras-pump-break ((arg0 kras-pump-break) (arg1 int))
  (process-nettable-method-34 self -1 (new 'static 'rgba :r #xff :g #xff))
  (process-nettable-method-35 self (the-as int arg0))
  (process-nettable-method-36 self)
  (the-as process 0)
  )

;; definition for method 41 of type kras-pump-break
(defmethod process-nettable-method-41 ((this kras-pump-break) (arg0 int))
  (let ((s5-0 (get-process *default-dead-pool* kras-pump-break #x4000 1)))
    (ppointer->process (when s5-0
                         (let ((t9-1 (method-of-type kras-pump-break activate))
                               (a0-2 s5-0)
                               (a1-2 *default-pool*)
                               (a2-1 "kras-pump-break")
                               (a3-1 #x70004000)
                               )
                           (t9-1 (the-as kras-pump-break a0-2) a1-2 a2-1 (the-as pointer a3-1))
                           (run-now-in-process s5-0 kras-pump-break-init-remote arg0 (the-as none a3-1))
                           )
                         (-> s5-0 ppointer)
                         )
                       )
    )
  )

;; definition for method 37 of type kras-pump-break
;; WARN: Return type mismatch int vs none.
(defmethod process-nettable-method-37 ((this kras-pump-break) (arg0 net-process-class-info))
  (set! (-> arg0 msg-map (-> arg0 msg-count)) 'explode)
  (+! (-> arg0 msg-count) 1)
  0
  (none)
  )

;; definition for symbol *msg-map-kras-pump-break*, type array
(define *msg-map-kras-pump-break*
  (the-as array (new 'static 'boxed-array :type symbol :length 0 :allocated-length 64))
  )

;; definition for symbol *net-process-class-info-kras-pump-break*, type net-process-class-info
(define *net-process-class-info-kras-pump-break* (new 'static 'net-process-class-info))

;; failed to figure out what this is:
(set! (-> *net-process-class-info-kras-pump-break* msg-map) (the-as (array symbol) *msg-map-kras-pump-break*))

;; failed to figure out what this is:
(write-msg-map-rec kras-pump-break *net-process-class-info-kras-pump-break*)

;; failed to figure out what this is:
(set! (-> *net-process-class-info-kras-pump-break* owner-type) kras-pump-break)

;; failed to figure out what this is:
(net-process-class-register *net-process-class-info-kras-pump-break*)

;; definition for method 40 of type kras-pump-break
(defmethod process-nettable-method-40 ((this kras-pump-break))
  *net-process-class-info-kras-pump-break*
  )

;; definition for method 17 of type kras-pump-break
;; WARN: Return type mismatch symbol vs none.
(defmethod process-nettable-method-17 ((this kras-pump-break) (arg0 symbol) (arg1 mem-buffer))
  (case arg0
    (('explode)
     (let ((a0-3 (handle->process (-> this drawable))))
       (if a0-3
           (send-event a0-3 'explode)
           )
       )
     (set! (-> this exploded) (the-as basic #t))
     )
    )
  (none)
  )

;; definition for method 15 of type kras-pump-break
;; og:preserve-this bring-up guard: retail writes (-> this exploded) into
;; the net message buffer through (method-of-type mem-buffer
;; mem-buffer-method-29). mem-buffer is a declare-type stub in goal_src
;; (mem-buffer-h is not landed), so the dispatch cannot compile ("The
;; method form is ambiguous when used on a forward declared type"),
;; matching the identical park already in jungle-obs.gc's
;; jungle-stone-snake-head process-nettable-method-15 override. Net
;; message pack cannot run before the net legs land, so the park is
;; behavior-neutral during bring-up.
(defmethod process-nettable-method-15 ((this kras-pump-break) (arg0 symbol) (arg1 mem-buffer))
  (none)
  )

;; definition for method 44 of type kras-pump-break
;; WARN: Return type mismatch int vs none.
;; ERROR: Failed store: (s.d! (+ v1-2 292) a0-10) at op 50
(defmethod kras-pump-break-method-44 ((this kras-pump-break))
  (let ((v1-2 (ppointer->process (process-spawn
                                   kras-pump-break-draw
                                   this
                                   (-> this entity)
                                   (-> this exploded)
                                   :name "kras-pump-break-draw"
                                   :to this
                                   )
                                 )
              )
        )
    (set! (-> this drawable) (process->handle v1-2))
    ;; og:preserve-this compile-only cast: v1-2 is a plain process here
    ;; (from ppointer->process), which does not support raw pointer
    ;; arithmetic; matches the (pointer uint8) reinterpret idiom already
    ;; used for raw-offset stores elsewhere this band.
    (let ((a0-10 (process->handle this)))
      (s.d! (&+ (the-as (pointer uint8) v1-2) 292) a0-10)
      )
    )
  0
  (none)
  )

;; definition for method 43 of type kras-pump-break
;; WARN: Return type mismatch int vs none.
(defmethod kras-pump-break-method-43 ((this kras-pump-break))
  (send-event (handle->process (-> this drawable)) 'go-die)
  (set! (-> this drawable) (the-as handle #f))
  0
  (none)
  )

;; definition for method 35 of type kras-pump-break
;; WARN: Return type mismatch object vs none.
(defmethod process-nettable-method-35 ((this kras-pump-break) (arg0 int))
  (stack-size-set! (-> this main-thread) 32)
  (logior! (-> this mask) (process-mask actor-pause))
  (if (not (process-nettable-method-25 this))
      (process-nettable-method-34 this 8 (new 'static 'rgba :r #xff :g #xff))
      )
  ;; og:preserve-this bring-up guard: retail reads the exploded flag off the
  ;; remote init message through (method-of-type mem-buffer
  ;; mem-buffer-method-46). mem-buffer is a declare-type stub in goal_src
  ;; (mem-buffer-h is not landed), so the dispatch cannot compile; re-derive
  ;; this line from the disasm when mem-buffer-h lands (jungle-obs.gc's
  ;; jungle-stone-snake-head-method-35 precedent). This method only runs for
  ;; net-remote spawns, which cannot happen before the net legs land, and #f
  ;; matches the local init-from-entity! default, so the park is
  ;; behavior-neutral.
  (set! (-> this exploded) #f)
  (set! (-> this drawable) (the-as handle #f))
  (go (method-of-object this idle))
  (none)
  )

;; definition for method 11 of type kras-pump-break
(defmethod init-from-entity! ((this kras-pump-break) (arg0 entity-actor))
  (stack-size-set! (-> this main-thread) 32)
  (logior! (-> this mask) (process-mask actor-pause))
  (if (not (process-nettable-method-25 this))
      (process-nettable-method-34 this 8 (new 'static 'rgba :r #xff :g #xff))
      )
  (set! (-> this drawable) (the-as handle #f))
  (set! (-> this exploded) #f)
  (go (method-of-object this idle))
  )

;; definition for method 45 of type kras-pump-break
;; og:preserve-this compile-only trailing-none removal: the decoder's own
;; WARN flagged this ("Return type mismatch symbol vs none"); with the
;; header retyped to symbol, the trailing (none) below no longer applies
;; and would discard the real if-expression's value.
(defmethod kras-pump-break-method-45 ((this kras-pump-break))
  (new 'stack-no-clear 'event-message-block)
  (let ((v1-2 (-> this entity extra)))
    (-> v1-2 level)
    (new 'stack-no-clear 'vector)
    (if (-> this level all-visible?)
        (vis-dist-ok? (-> this entity trans) (-> v1-2 vis-dist))
        (vis-cull-all? (-> this level) (-> v1-2 vis-id))
        )
    )
  )

;; failed to figure out what this is:
(defstate idle (kras-pump-break)
  :virtual #t
  :event (behavior ((proc process) (argc int) (message symbol) (block event-message-block))
    (case message
      ;; og:preserve-this bring-up parking: the net-replication sequence
      ;; below cannot compile against the current process-nettable stubs.
      ;; Methods 27/28/29 return none (process-nettable.gc stubs), so
      ;; binding method-27's result and threading it into method-29 fails
      ;; codegen on a NullType. Parked verbatim rather than rewritten,
      ;; matching the parked send section in jungle-obs.gc's
      ;; jungle-stone-snake-head idle 'explode handler. Retail sequence:
      ;; method-27 takes 'explode (begin message) and returns the buffer
      ;; handle; method-29 takes that return plus -2 and 64 (send);
      ;; method-28 takes the same return (end message).
      (('explode)
       ;; (let ((gp-0 (process-nettable-method-27 self 'explode)))
       ;;   (process-nettable-method-29 self gp-0 -2 64)
       ;;   )
       ;; ((method-of-object self process-nettable-method-28))
       (let ((v0-0 #t))
         (set! (-> self exploded) (the-as basic v0-0))
         v0-0
         )
       )
      )
    )
  :code sleep-code
  :post (behavior ()
    (cond
      ((and (kras-pump-break-method-45 self) (not (handle->process (-> self drawable))))
       (kras-pump-break-method-44 self)
       )
      ((and (not (kras-pump-break-method-45 self)) (handle->process (-> self drawable)))
       (kras-pump-break-method-43 self)
       )
      )
    )
  )

;; failed to figure out what this is:
(defskelgroup skel-kras-pump-break kras-pump-break kras-pump-break-lod0-jg kras-pump-break-idle-ja
              ((kras-pump-break-lod0-mg (meters 999999)))
              :bounds (static-spherem 0 5 0 7)
              :origin-joint-index 3
              )

;; failed to figure out what this is:
(defskelgroup skel-kras-pump-debris-a kras-pump-debris kras-pump-debris-a-lod0-jg -1
              ((kras-pump-debris-a-lod0-mg (meters 999999)))
              :bounds (static-spherem 0 0 0 5)
              )

;; failed to figure out what this is:
(defskelgroup skel-kras-pump-debris-b kras-pump-debris kras-pump-debris-b-lod0-jg -1
              ((kras-pump-debris-b-lod0-mg (meters 999999)))
              :bounds (static-spherem 0 0 0 5)
              )

;; failed to figure out what this is:
(defskelgroup skel-kras-pump-debris-c kras-pump-debris kras-pump-debris-c-lod0-jg -1
              ((kras-pump-debris-c-lod0-mg (meters 999999)))
              :bounds (static-spherem 0 0 0 5)
              )

;; definition for symbol *kras-pump-debris-params*, type debris-static-params
(define *kras-pump-debris-params*
  (new 'static 'debris-static-params
    :joints (new 'static 'boxed-array :type debris-static-joint-params
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-kras-pump-debris-c"
        :offset (new 'static 'vector :data (new 'static 'array float 4 0.0 0.0 0.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-kras-pump-debris-c"
        :offset (new 'static 'vector :data (new 'static 'array float 4 0.0 8192.0 0.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-kras-pump-debris-c"
        :offset (new 'static 'vector :data (new 'static 'array float 4 0.0 12288.0 0.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-kras-pump-debris-a"
        :offset (new 'static 'vector :data (new 'static 'array float 4 0.0 16384.0 0.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-kras-pump-debris-a"
        :offset (new 'static 'vector :data (new 'static 'array float 4 0.0 4096.0 0.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-kras-pump-debris-a"
        :offset (new 'static 'vector :data (new 'static 'array float 4 0.0 4096.0 0.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-kras-pump-debris-b"
        :offset (new 'static 'vector :data (new 'static 'array float 4 0.0 8192.0 0.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-kras-pump-debris-b"
        :offset (new 'static 'vector :data (new 'static 'array float 4 0.0 12288.0 0.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-kras-pump-debris-b"
        :offset (new 'static 'vector :data (new 'static 'array float 4 0.0 4096.0 0.0 1.0))
        )
      (new 'static 'debris-static-joint-params
        :parent-joint-index 3
        :group "skel-kras-pump-debris-b"
        :offset (new 'static 'vector :data (new 'static 'array float 4 0.0 16384.0 0.0 1.0))
        )
      )
    :collide-spec (collide-spec backgnd)
    :sound-hit #x22d
    :art-level #f
    :vscale 20480.0
    :volume-time 2.0
    :play-delay 45
    :min-speed 8192.0
    :max-speed 40960.0
    )
  )

;; failed to figure out what this is:
(defstate explode (kras-pump-break-draw)
  :virtual #t
  ;; og:preserve-this: the retail :event handler decoded as a raw label
  ;; (L11), same defect class as this file's other omitted raw-label state
  ;; slots.
  :enter (behavior ()
    (call-parent-state-handler enter)
    (setup-masks (-> self draw) 1 2)
    (let ((v1-5 (make-sound-instance (-> *sound-info* 526) (new 'static 'sound-id) (the-as handle #f))))
      (set! (-> self flame-sound) (the-as uint (if v1-5
                                                   (the-as int (-> v1-5 id))
                                                   0
                                                   )
                                          )
            )
      )
    ;; og:preserve-this compile-only casts: v1-9 is a collide-shape-prim,
    ;; which does not support raw pointer arithmetic; each store below
    ;; targets the same field 80 bytes further into a sibling prim (108,
    ;; 188, 268, 348, 428 are all +28 within consecutive 80-byte prims),
    ;; so each raw offset is reinterpreted through (pointer uint8).
    (let ((v1-9 (&+ (-> self root root-prim) 80)))
      (set! (-> v1-9 prim-core collide-as) (collide-spec))
      (s.w! (&+ (the-as (pointer uint8) v1-9) 108) 0)
      (let ((a0-3 #x120040))
        (s.w! (&+ (the-as (pointer uint8) v1-9) 188) a0-3)
        )
      (let ((a0-4 #x120040))
        (s.w! (&+ (the-as (pointer uint8) v1-9) 268) a0-4)
        )
      (let ((a0-5 #x120040))
        (s.w! (&+ (the-as (pointer uint8) v1-9) 348) a0-5)
        )
      (let ((v0-4 #x120040))
        (s.w! (&+ (the-as (pointer uint8) v1-9) 428) v0-4)
        )
      )
    )
  :exit (behavior ()
    (sound-stop (the-as sound-id (-> self flame-sound)) #t)
    )
  :code sleep-code
  :post (behavior ()
    (if (nonzero? (-> self part))
        (spawn-from-mat (-> self part) (-> self node-list data 3 bone transform))
        )
    )
  )

;; definition for method 64 of type kras-pump-break-draw
;; WARN: Return type mismatch (pointer process) vs none.
(defmethod kras-pump-break-proxy-method-64 ((this kras-pump-break-draw))
  (rlet ((vf0 :class vf)
         (vf4 :class vf)
         (vf5 :class vf)
         (vf6 :class vf)
         )
    (init-vf0-vector)
    (let ((v1-1 (make-sound-instance (-> *sound-info* 527) (new 'static 'sound-id) (the-as handle #f))))
      (if v1-1
          (-> v1-1 id)
          0
          )
      )
    (let ((a1-2 (new 'stack 'debris-tuning (the-as uint 0))))
      (set! (-> a1-2 scale-rand-lo) 1.0)
      (set! (-> a1-2 scale-rand-hi) 2.0)
      (set-vector! (-> a1-2 fountain-rand-transv-lo) -122880.0 122880.0 -122880.0 1.0)
      (let ((a2-2 (-> a1-2 fountain-rand-transv-lo)))
        (let ((v1-8 (-> a1-2 fountain-rand-transv-lo))
              (a0-7 (-> this impact-velocity))
              )
          (.lvf vf4 (&-> v1-8 quad))
          (.lvf vf5 (&-> a0-7 quad))
          )
        (.mov.vf.w vf6 vf0)
        (.add.vf.xyz vf6 vf4 vf5)
        (.svf (&-> a2-2 quad) vf6)
        )
      (set-vector! (-> a1-2 fountain-rand-transv-hi) 122880.0 368640.0 122880.0 1.0)
      (let ((a2-3 (-> a1-2 fountain-rand-transv-hi)))
        (let ((v1-10 (-> a1-2 fountain-rand-transv-hi))
              (a0-12 (-> this impact-velocity))
              )
          (.lvf vf4 (&-> v1-10 quad))
          (.lvf vf5 (&-> a0-12 quad))
          )
        (.mov.vf.w vf6 vf0)
        (.add.vf.xyz vf6 vf4 vf5)
        (.svf (&-> a2-3 quad) vf6)
        )
      (set! (-> a1-2 fountain-rand-transv-hi w) 16384.0)
      (set! (-> a1-2 duration) (seconds 4))
      (debris-spawn this a1-2 *kras-pump-debris-params* (the-as process-drawable #f))
      )
    (set! (-> *part-tracker-params-default* group) (-> *part-group-id-table* 394))
    (vector-reset! (-> *part-tracker-params-default* local-offset))
    (set! (-> *part-tracker-params-default* duration) (the-as uint 1500))
    (set! (-> *part-tracker-params-default* callback) #f)
    (set! (-> *part-tracker-params-default* userdata) (the-as uint #f))
    (set! (-> *part-tracker-params-default* target) #f)
    (set! (-> *part-tracker-params-default* mat-joint) (-> this node-list data 3 bone transform))
    (set! (-> *part-tracker-params-default* local-space-param) 0)
    (set! (-> *part-tracker-params-default* local-space-callback) #f)
    (set! (-> *part-tracker-params-default* subsampler-num) 1.0)
    *part-tracker-params-default*
    (if (logtest? (-> *part-group-id-table* 394 flags) (sp-group-flag sp13))
        (process-spawn
          part-tracker-subsampler
          :init part-tracker-subsampler-init
          *part-tracker-params-default*
          :name "part-tracker-subsampler"
          :to *entity-pool*
          :unk 0
          )
        (process-spawn
          part-tracker
          :init part-tracker-init
          *part-tracker-params-default*
          :name "part-tracker"
          :to *entity-pool*
          :unk 0
          )
        )
    (none)
    )
  )

;; definition for method 63 of type kras-pump-break-draw
;; WARN: Return type mismatch int vs none.
(defmethod kras-pump-break-proxy-method-63 ((this kras-pump-break-draw) (arg0 entity-actor))
  (stack-size-set! (-> this main-thread) 32)
  (logior! (-> this mask) (process-mask actor-pause))
  (let ((s4-0 (new 'process 'collide-shape this (collide-list-enum usually-hit-by-player))))
    (let ((s3-0 (new 'process 'collide-shape-prim-group s4-0 (the-as uint 6) 0)))
      (set! (-> s4-0 total-prims) (the-as uint 7))
      (set! (-> s3-0 prim-core collide-as) (collide-spec obstacle camera-blocker pusher))
      (set! (-> s3-0 prim-core collide-with) (collide-spec jak bot player-list))
      (set! (-> s3-0 prim-core action) (collide-action solid))
      (set! (-> s3-0 transform-index) 3)
      (set-vector! (-> s3-0 local-sphere) 0.0 20480.0 20480.0 69632.0)
      (set! (-> s4-0 root-prim) s3-0)
      )
    (pusher-init s4-0)
    (let ((v1-15 (new 'process 'collide-shape-prim-mesh s4-0 (the-as uint 0) (the-as uint 0))))
      (set! (-> v1-15 prim-core collide-as) (collide-spec obstacle camera-blocker pusher))
      (set! (-> v1-15 prim-core collide-with) (collide-spec jak bot player-list))
      (set! (-> v1-15 prim-core action) (collide-action solid))
      (set! (-> v1-15 transform-index) 3)
      (set-vector! (-> v1-15 local-sphere) 0.0 20480.0 0.0 28672.0)
      )
    (let ((v1-17 (new 'process 'collide-shape-prim-mesh s4-0 (the-as uint 1) (the-as uint 1))))
      (set! (-> v1-17 prim-core collide-as) (collide-spec))
      (set! (-> v1-17 prim-core collide-with) (collide-spec jak bot player-list))
      (set! (-> v1-17 prim-core action) (collide-action solid))
      (set! (-> v1-17 transform-index) 3)
      (set-vector! (-> v1-17 local-sphere) 0.0 20480.0 0.0 28672.0)
      )
    (let ((v1-19 (new 'process 'collide-shape-prim-sphere s4-0 (the-as uint 2))))
      (set! (-> v1-19 prim-core collide-as) (collide-spec))
      (set! (-> v1-19 prim-core collide-with) (collide-spec jak bot player-list))
      (set! (-> v1-19 prim-core action) (collide-action dont-push-away))
      (set! (-> v1-19 transform-index) 3)
      (set-vector! (-> v1-19 local-sphere) 0.0 8192.0 8192.0 8192.0)
      )
    (let ((v1-21 (new 'process 'collide-shape-prim-sphere s4-0 (the-as uint 3))))
      (set! (-> v1-21 prim-core collide-as) (collide-spec))
      (set! (-> v1-21 prim-core collide-with) (collide-spec jak bot player-list))
      (set! (-> v1-21 prim-core action) (collide-action dont-push-away))
      (set! (-> v1-21 transform-index) 3)
      (set-vector! (-> v1-21 local-sphere) 0.0 10240.0 24576.0 10240.0)
      )
    (let ((v1-23 (new 'process 'collide-shape-prim-sphere s4-0 (the-as uint 4))))
      (set! (-> v1-23 prim-core collide-as) (collide-spec))
      (set! (-> v1-23 prim-core collide-with) (collide-spec jak bot player-list))
      (set! (-> v1-23 prim-core action) (collide-action dont-push-away))
      (set! (-> v1-23 transform-index) 3)
      (set-vector! (-> v1-23 local-sphere) 0.0 12288.0 40960.0 12288.0)
      )
    (let ((v1-25 (new 'process 'collide-shape-prim-sphere s4-0 (the-as uint 5))))
      (set! (-> v1-25 prim-core collide-as) (collide-spec))
      (set! (-> v1-25 prim-core collide-with) (collide-spec jak bot player-list))
      (set! (-> v1-25 prim-core action) (collide-action dont-push-away))
      (set! (-> v1-25 transform-index) 3)
      (set-vector! (-> v1-25 local-sphere) 0.0 18432.0 57344.0 20480.0)
      )
    (set! (-> s4-0 nav-radius) (* 0.75 (-> s4-0 root-prim local-sphere w)))
    (let ((v1-28 (-> s4-0 root-prim)))
      (set! (-> s4-0 backup-collide-as) (-> v1-28 prim-core collide-as))
      (set! (-> s4-0 backup-collide-with) (-> v1-28 prim-core collide-with))
      )
    (set! (-> s4-0 event-self) 'touched)
    (set! (-> this root) s4-0)
    )
  (set! (-> this part) (create-launch-control (-> *part-group-id-table* 395) this))
  (set! (-> this hit-points) 10.0)
  (initialize-skeleton
    this
    (the-as skeleton-group (art-group-get-by-name *level* "skel-kras-pump-break" (the-as (pointer level) #f)))
    (the-as pair 0)
    )
  (process-drawable-from-entity! this arg0)
  (setup-masks (-> this draw) 2 1)
  (ja-post)
  0
  (none)
  )




